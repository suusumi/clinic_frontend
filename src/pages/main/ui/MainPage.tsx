import {Box} from "@mui/material";
import {Header} from "src/widgets/header";

export const MainPage = () => {
    return (
        <Box>
            <Header></Header>
            MainPage
        </Box>
    );
};
